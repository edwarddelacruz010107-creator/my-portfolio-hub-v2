# ─── Google OAuth production setup ─────────────────────────────
# Create/select a Web application OAuth client in Google Cloud Console.
# Do NOT commit real secrets. Set these values only in your production host.
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Recommended: one shared callback for both Sign In and Create Account.
# Add this exact URI in Google Cloud Console Authorized redirect URIs.
APP_BASE_URL=https://myportfoliohub.online
GOOGLE_OAUTH_REDIRECT_URI=https://myportfoliohub.online/auth/google/callback

# Optional compatibility URIs if needed in Google Console:
# https://myportfoliohub.online/auth/google/signin/callback
# https://myportfoliohub.online/auth/google/signup/callback

# ─── Cloudinary persistent image storage ───────────────────────
# Recommended while Supabase Storage is unavailable. Never commit real secrets.
STORAGE_PROVIDER=cloudinary
USE_CLOUDINARY_STORAGE=true
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
CLOUDINARY_FOLDER_ROOT=myportfoliohub

# Alternative single-variable configuration supported by the app:
# CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME

# Keep the existing WebP optimizer enabled before Cloudinary upload.
CONVERT_UPLOADS_TO_WEBP=true
UPLOAD_WEBP_QUALITY=82
UPLOAD_IMAGE_MAX_DIMENSION=2048


# Company credibility / public legal pages
COMPANY_NAME=MyPortfolioHub
SUPPORT_EMAIL=hello@myportfoliohub.online
COMPANY_LOCATION=Philippines
# Optional comma-separated official social profile URLs for Organization schema.
COMPANY_SOCIAL_URLS=
# Optional real, visibly published software review. Leave blank otherwise.
SOFTWARE_REVIEW_BODY=
SOFTWARE_REVIEW_AUTHOR=
SOFTWARE_REVIEW_RATING=

# ─── Billing currency conversion ───────────────────────────────
# Store all authoritative plan prices in USD and convert only for display/payment.
# Never commit the real API key.
CURRENCY_PROVIDER=freecurrencyapi
FREECURRENCYAPI_KEY=your-regenerated-freecurrencyapi-key

# Optional alternative providers:
# CURRENCY_PROVIDER=frankfurter
# CURRENCYAPI_KEY=your-currencyapi-key

# Dodo Payments (start in test mode)
DODO_PAYMENTS_ENABLED=true
DODO_PAYMENTS_MODE=test
DODO_PAYMENTS_API_KEY=
DODO_PAYMENTS_WEBHOOK_SECRET=
DODO_BASIC_MONTHLY_PRODUCT_ID=
DODO_BASIC_YEARLY_PRODUCT_ID=
DODO_PRO_MONTHLY_PRODUCT_ID=
DODO_PRO_YEARLY_PRODUCT_ID=
DODO_ENTERPRISE_MONTHLY_PRODUCT_ID=
DODO_ENTERPRISE_YEARLY_PRODUCT_ID=
# Webhook URL: https://myportfoliohub.online/webhooks/dodo
