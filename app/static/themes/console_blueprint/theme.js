(function(){
  'use strict';
  document.documentElement.classList.add('js-ready');
  var root=document.documentElement;
  var storageKey='myportfoliohub-developer-journal-theme';
  function storedTheme(){try{return localStorage.getItem(storageKey);}catch(e){return null;}}
  function setTheme(theme){root.setAttribute('data-theme',theme);var toggle=document.getElementById('themeToggle');if(toggle)toggle.setAttribute('aria-pressed',theme==='dark'?'true':'false');try{localStorage.setItem(storageKey,theme);}catch(e){}}
  var preferred=storedTheme()||(window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light');setTheme(preferred);
  document.addEventListener('DOMContentLoaded',function(){
    var year=document.getElementById('copyYear');if(year)year.textContent=String(new Date().getFullYear());
    var themeToggle=document.getElementById('themeToggle');if(themeToggle)themeToggle.addEventListener('click',function(){setTheme(root.getAttribute('data-theme')==='dark'?'light':'dark');});
    var nav=document.getElementById('siteNav'),navToggle=document.getElementById('navToggle'),mobileMenu=document.getElementById('mobileMenu');
    function closeMenu(){if(!mobileMenu)return;mobileMenu.classList.remove('is-open');mobileMenu.setAttribute('aria-hidden','true');document.body.classList.remove('menu-open');if(navToggle)navToggle.setAttribute('aria-expanded','false');}
    if(navToggle&&mobileMenu)navToggle.addEventListener('click',function(){var open=!mobileMenu.classList.contains('is-open');mobileMenu.classList.toggle('is-open',open);mobileMenu.setAttribute('aria-hidden',open?'false':'true');document.body.classList.toggle('menu-open',open);navToggle.setAttribute('aria-expanded',open?'true':'false');});
    document.querySelectorAll('[data-nav]').forEach(function(link){link.addEventListener('click',closeMenu);});
    document.addEventListener('keydown',function(e){if(e.key==='Escape'){closeMenu();closeCertificate();}});
    var sections=Array.prototype.slice.call(document.querySelectorAll('section[id]')),navLinks=Array.prototype.slice.call(document.querySelectorAll('.nav-links [data-nav]'));
    function scrollUI(){if(nav)nav.classList.toggle('is-scrolled',window.scrollY>8);var current='';sections.forEach(function(section){if(window.scrollY+110>=section.offsetTop)current=section.id;});navLinks.forEach(function(link){link.classList.toggle('is-active',link.getAttribute('href')==='#'+current);});var top=document.getElementById('backToTop');if(top)top.classList.toggle('is-visible',window.scrollY>500);}
    window.addEventListener('scroll',scrollUI,{passive:true});scrollUI();
    var back=document.getElementById('backToTop');if(back)back.addEventListener('click',function(){window.scrollTo({top:0,behavior:'smooth'});});
    var reveals=document.querySelectorAll('.reveal');if('IntersectionObserver'in window){var io=new IntersectionObserver(function(entries){entries.forEach(function(entry){if(entry.isIntersecting){entry.target.classList.add('is-visible');io.unobserve(entry.target);}});},{threshold:.12});reveals.forEach(function(el){io.observe(el);});}else{reveals.forEach(function(el){el.classList.add('is-visible');});}
    document.querySelectorAll('[data-count]').forEach(function(counter){var target=Number(counter.dataset.count);if(!Number.isFinite(target)||target<=0)return;var started=false;function animate(){if(started)return;started=true;var start=performance.now(),duration=700;function frame(now){var progress=Math.min((now-start)/duration,1);counter.textContent=String(Math.round(target*(1-Math.pow(1-progress,3))));if(progress<1)requestAnimationFrame(frame);}requestAnimationFrame(frame);}if('IntersectionObserver'in window){var countObserver=new IntersectionObserver(function(entries){entries.forEach(function(entry){if(entry.isIntersecting){animate();countObserver.disconnect();}});},{threshold:.5});countObserver.observe(counter);}else{animate();}});
    document.querySelectorAll('.filter-btn').forEach(function(button){button.addEventListener('click',function(){document.querySelectorAll('.filter-btn').forEach(function(item){item.classList.remove('is-active');});button.classList.add('is-active');var filter=button.dataset.filter;document.querySelectorAll('.project-card').forEach(function(card){card.classList.toggle('is-hidden',filter!=='all'&&card.dataset.category!==filter);});});});
    var slides=document.getElementById('testiSlides'),dots=document.getElementById('testiDots'),prev=document.getElementById('testiPrev'),next=document.getElementById('testiNext'),active=0,count=slides?slides.children.length:0;
    function go(index){if(!count)return;active=(index+count)%count;slides.style.transform='translateX(-'+active*100+'%)';if(dots)Array.prototype.forEach.call(dots.children,function(dot,i){dot.classList.toggle('is-active',i===active);});}
    if(count&&dots){for(var i=0;i<count;i++){var dot=document.createElement('button');dot.type='button';dot.className='testi-dot'+(i===0?' is-active':'');dot.setAttribute('aria-label','Go to testimonial '+(i+1));dot.dataset.index=String(i);dots.appendChild(dot);}dots.addEventListener('click',function(e){var target=e.target.closest('[data-index]');if(target)go(Number(target.dataset.index));});}
    if(prev)prev.addEventListener('click',function(){go(active-1);});if(next)next.addEventListener('click',function(){go(active+1);});
    var form=document.getElementById('contactForm'),status=document.getElementById('formStatus');function show(type,message){if(!status)return;status.className='form-status show '+type;status.textContent=message;}
    if(form)form.addEventListener('submit',function(e){e.preventDefault();if(!form.checkValidity()){form.reportValidity();return;}if(!form.action||form.getAttribute('action')==='#'){show('error','Contact form is not configured yet.');return;}var button=form.querySelector('button[type="submit"]'),original=button?button.textContent:'';if(button){button.disabled=true;button.textContent='Sending…';}fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}}).then(function(response){return response.json().catch(function(){return{status:response.ok?'success':'error',message:response.ok?'Message sent.':'Submission failed.'};}).then(function(payload){if(!response.ok||payload.status==='error')throw new Error(payload.message||'Submission failed.');return payload;});}).then(function(payload){show('success',payload.message||'Message sent successfully.');form.reset();}).catch(function(error){show('error',error.message||'Submission failed. Please try again.');}).finally(function(){if(button){button.disabled=false;button.textContent=original;}});});
  });
  var overlay=document.getElementById('certModalOverlay');
  function closeCertificate(){if(!overlay)return;overlay.classList.remove('is-open');overlay.setAttribute('aria-hidden','true');document.body.classList.remove('menu-open');}
  document.addEventListener('click',function(e){var card=e.target.closest('[data-cert-title]');if(card&&overlay){var title=document.getElementById('certModalTitle'),image=document.getElementById('certModalImage'),desc=document.getElementById('certModalDesc'),issuer=document.getElementById('certModalIssuer'),credential=document.getElementById('certModalCredential'),skills=document.getElementById('certModalSkills'),verify=document.getElementById('certModalVerify');if(title)title.textContent=card.dataset.certTitle||'Certificate';if(desc)desc.textContent=card.dataset.certDescription||'';if(issuer)issuer.textContent=card.dataset.certIssuer||'';if(credential){credential.textContent=card.dataset.certId?'Credential ID: '+card.dataset.certId:'';credential.hidden=!card.dataset.certId;}if(skills){skills.textContent='';(card.dataset.certSkills||'').split(',').map(function(item){return item.trim();}).filter(Boolean).forEach(function(item){var tag=document.createElement('span');tag.className='tech-tag';tag.textContent=item;skills.appendChild(tag);});skills.hidden=!skills.children.length;}if(verify){if(card.dataset.certUrl){verify.href=card.dataset.certUrl;verify.hidden=false;}else{verify.hidden=true;verify.removeAttribute('href');}}if(image){if(card.dataset.certImage){image.src=card.dataset.certImage;image.alt=(card.dataset.certTitle||'Certificate')+' image';image.hidden=false;}else{image.hidden=true;image.removeAttribute('src');}}overlay.classList.add('is-open');overlay.setAttribute('aria-hidden','false');document.body.classList.add('menu-open');var close=document.getElementById('certModalClose');if(close)close.focus();}
    if(e.target.closest('#certModalClose')||(overlay&&e.target===overlay))closeCertificate();
  });
})();
